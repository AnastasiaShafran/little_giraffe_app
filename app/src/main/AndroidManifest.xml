<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.anastasia.myfirstactivityproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".child.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".child.GiraffesList" />
        <activity android:name=".child.ChildrenProfileActivity" />
        <activity android:name=".child.MoreInfoActivity" />
        <activity android:name=".teacher.AddTeacherActivity" />
        <activity android:name=".teacher.WeeklySchedule" />
        <activity android:name=".teacher.TeacherListActivity" />
        <activity android:name=".teacherapp.TeacherEntranceActivity" />
        <activity android:name=".teacher.TeacherSchedule"></activity>
    </application>

</manifest>
