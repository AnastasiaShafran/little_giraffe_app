<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.anastasia.myfirstactivityproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ProfileChildren.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ProfileChildren.GiraffesList" />
        <activity android:name=".ProfileChildren.ChildrenProfileActivity" />
        <activity android:name=".ProfileChildren.MoreInfoActivity" />
        <activity android:name=".ProfileChildren.FragmentUpdate" />
        <activity android:name=".ProfileTeacher.AddTeacherActivity"></activity>
    </application>

</manifest>
